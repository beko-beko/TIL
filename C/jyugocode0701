```c
#include <stdio.h>

int main(void)
{
	int len;

	puts("左上直角二等辺三角形を表示します。");
	printf("短辺：");
	fflush(0);
	scanf("%d", &len);

	for (int i = len; i >= 1; i--) { // i行（i = 1, 2, … , len)
		for (int j = 1; j <= i; j++) { // 各行にi個の'*'を表示
			printf("*");
		}
		putchar('\n'); // 改行
	}

	return 0;
}

i <= len
3     3  i--
2     3
```
